name: Email Action Test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f3002303@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Finish step
        run: echo "Action completed"
